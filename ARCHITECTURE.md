# 架构核查与实现边界

目标架构是「微信小程序 → Cloud API ← Event Sync ← 多个 Android Terminal」。每个 Terminal 均为可独立营业的安卓机顶盒 APP，以本机 SQLite 作为账本事实源。门店不需要电脑。

| 环节 | 当前实现 | 后续工作 |
| --- | --- | --- |
| Terminal A/B | 每台 APK 自动生成独立门店、设备 ID；本机会员、卡、余额、次数、积分和流水 | 门店绑定、设备注册、员工权限 |
| 本地账本 | Android SQLite、追加流水、事务内幂等回执及 outbox，断网可营业 | 一致性备份、换机恢复、实机断电验收 |
| Event Sync | 独立网络线程每 30 秒上传最多 100 条，按 sequence 排序；确认完整才标记 SYNCED | 指数退避、错误诊断、下载游标、冲突处理 |
| Cloud API | Docker 服务、原始事件保存、会员/卡/积分只读投影 | 正式设备认证、监控与扩容 |
| 微信小程序 | 预留完整 API 契约；提供内部联调 GET /api/v1/miniapp/me | 微信登录、会员绑定、预约、客户端 |
| GitHub | APK 构建与 API 19/28 测试，Docker 构建与启动验证 | 签名发行 APK、镜像发布 |

## 数据流

1. A 的本地业务命令一次事务提交业务数据、财务流水、命令回执及事件。B 同样独立运行。
2. 每台设备把 `sync_event.id` 映射为 `event_id`，上传事件对象；不上传 SQLite 文件，也不连接其他终端数据库。
3. 云端一个事务完成原始事件保存及投影更新；相同事件返回原确认，不同内容复用事件 ID 则整批拒绝。每设备 sequence 从 1 连续递增，缺口和逆序新事件拒绝，旧事件重试不倒退投影。
4. 终端只在完整验证 `ACCEPTED` / `ALREADY_ACCEPTED` 回执后标记该批次。断网、超时及确认丢失均保留原 ID 重试。
5. 小程序未来通过登录会话读取云端投影，展示最后同步时间；不能调用终端交易 API 或直接改余额。

当前 A/B 是独立账本，不提供同一余额在多台离线设备上消费的能力。云端实体绑定首个来源终端，拒绝另一来源覆盖；这项一致性约束不代替正式设备鉴权。每台首次安装默认独立 shop_id，不能将此版本当作同店共享账本或会员跨店通兑系统。

云端使用标准库 HTTP 服务和 SQLite 作为可运行骨架，单进程部署。内部读取接口使用共享联调令牌和显式会员 ID，尚未绑定微信身份；上线前必须完成设备和会员授权。云端事件投影不等同完整备份，不能用于现阶段的设备换机恢复。
